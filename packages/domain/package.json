{
  "name": "@golab/domain",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@golab/config": "workspace:*",
    "@golab/contracts": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
